# Roadmap

## Now
- V2.3 release: dispatcher + packaging + roadmap microcopy shipped
- Release snapshot (screenshot + release note)

## Next
- V2.4: optional roadmap text expansion (Leo) + UI polish
- V2.5: agent inbox runner (auto-consume) if needed

## Risks
- Scope creep (keep V2 as small, testable PRs)
- Memory contamination (cross-project) if retrieval is not scoped
- Schema drift (state.json / phases) if not enforced by tests
