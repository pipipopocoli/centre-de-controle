# WAVE09 Decision - 2026-02-20T21:48Z

## Input reviewed
- Global chat (repo root): latest active signal is Nova Wave09 PLAN kickoff with explicit P0 recommendation.
- Requests ledger:
  - repo root: 7 open-like entries (current wave mentions + owner routing)
  - AppSupport root: historical queued noise in ledger, but runtime open set is reconciled to 0.
- Healthchecks:
  - repo root: healthy
  - AppSupport root: healthy (after forced KPI snapshot refresh)

## Decision
- Adopt Nova P0 recommendation.
- Execution order is locked:
  1. CP-0035 (dual-root cadence lock) - immediate
  2. CP-0036 (healthcheck deterministic hardening) - parallel with CP-0035
  3. CP-0037 (Pilotage control badges) - starts after first green checkpoint
  4. CP-0038 (advisory ledger closeout) - continuous, with final lock at closeout

## Reasoning
- Primary operational risk was `stale_kpi_snapshot` on AppSupport, not queue integrity.
- This can reappear without cadence discipline, so cadence and health contract hardening must come first.
- UI and advisory should not proceed as if control is stable until backend cadence is locked.

## Immediate operator actions
1. Send Wave09 lead prompts to `@victor`, `@leo`, `@nova`.
2. Wait 15 minutes for lead ack (`Now/Next/Blockers`).
3. Send specialist prompts to `@agent-1`, `@agent-3`, `@agent-6`, `@agent-7`.
4. Run cadence commands every 30-45 minutes until T+2h checkpoint.

## Gate to continue
- Continue to UI/advisory closeout only if both roots remain healthy across two consecutive checks.
