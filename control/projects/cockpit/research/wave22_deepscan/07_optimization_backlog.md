# Wave22 Optimization Backlog

## P0 fix now
- Remove active legacy launcher references from README, QUICKSTART, runbook, and installation docs.
- Audit and correct stale endpoint docs that still advertise `chat/agentic-turn` and `wizard-live` in active surfaces.
- Reduce `demo` as a default teaching path in active docs and tests where it pollutes current product understanding.
- Finish direct lane vs room lane reliability and degraded honesty.
- Lock one operator-facing truth path for the live app, docs, and runtime.

## P1 next wave
- Decompose or archive stale cloud/server protocol docs if desktop-local runtime is the only daily path.
- Move historical proofs deeper behind archive/index pages.
- Remove remaining legacy strings in service prompts and UI notes.
- Add a proper repo health script to CI or pre-release checks.
- Normalize Android and Python compatibility surfaces so they clearly document when they diverge from the desktop-local runtime.
- Add explicit owner + done criteria to each Wave22 issue for stricter merge discipline.

## Archive or drop
- legacy launchers in active docs
- old `Centre de controle` build references outside archival receipts
- stale examples that point to `demo` or old agentic endpoints
- obsolete assets/scripts that are no longer in the live desktop path
